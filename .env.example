# Three-Stage RAG Integrity Shield Configuration
PROJECT_NAME="Three-Stage RAG Integrity Shield"
VERSION="1.0.0"
HOST="0.0.0.0"
PORT=8000
DEBUG=True
ENVIRONMENT="production"

# LLM Configuration (Default is deterministic mock for 100% reliable judging)
LLM_PROVIDER="deterministic_mock"
# OPENAI_API_KEY="sk-..."
# GEMINI_API_KEY="..."

# Frontend API URL (Used when frontend is deployed on Vercel and backend is hosted separately)
VITE_API_BASE_URL=""
